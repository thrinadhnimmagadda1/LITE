services:
  # Frontend Static Site
  - type: static
    name: lite-frontend
    env: node
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: NODE_VERSION
        value: 18.x
      - key: REACT_APP_API_URL
        value: https://lite-backend.onrender.com/api
    plan: free

  # Backend Service
  - type: web
    name: lite-backend
    env: python3
    buildCommand: |
      pip install -r backend/requirements.txt
      python backend/manage.py migrate
      python backend/manage.py collectstatic --noinput
    startCommand: gunicorn config.wsgi:application --chdir backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: DEBUG
        value: 'False'
      - key: SECRET_KEY
        generateValue: true
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings
    plan: free

  # Database (if using SQLite, this is just for reference - consider using Render's PostgreSQL instead)
  # - type: postgres
  #   name: lite-db
  #   plan: free
